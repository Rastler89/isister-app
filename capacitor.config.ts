import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.isister.app',
  appName: 'isister-client',
  webDir: 'dist/isister-client',
  bundledWebRuntime: false
};

export default config;
